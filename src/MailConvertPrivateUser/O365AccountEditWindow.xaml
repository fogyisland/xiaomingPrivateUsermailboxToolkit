<Window x:Class="MailConvertPrivateUser.O365AccountEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:MailConvertPrivateUser.Markup"
        Title="{loc:Loc O365Edit_Title}" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="SecondaryHoverBrush" Color="#475569"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#64748B"/>
        <SolidColorBrush x:Key="HelpBackground" Color="#F8FAFC"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SecondaryHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20,16">
            <StackPanel>
                <TextBlock Text="{loc:Loc O365Edit_Group}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="{loc:Loc O365Edit_GroupDesc}" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,16">
            <StackPanel>
                <!-- 名称 -->
                <TextBlock Text="{loc:Loc O365Edit_Field_Name}" FontSize="12" FontWeight="Medium" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                <TextBox x:Name="txtName"/>
                <TextBlock Text="{loc:Loc O365Edit_Field_NameDesc}" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,16"/>

                <!-- Tenant ID -->
                <TextBlock Text="Tenant ID" FontSize="12" FontWeight="Medium" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                <TextBox x:Name="txtTenantId"/>
                <TextBlock Text="{loc:Loc O365Edit_Field_TenantIdDesc}" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,16"/>

                <!-- Client ID -->
                <TextBlock Text="Client ID" FontSize="12" FontWeight="Medium" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                <TextBox x:Name="txtClientId"/>
                <TextBlock Text="{loc:Loc O365Edit_Field_ClientIdDesc}" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,16"/>

                <!-- 用户名 -->
                <TextBlock Text="{loc:Loc O365Edit_Field_Username}" FontSize="12" FontWeight="Medium" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                <TextBox x:Name="txtUsername"/>
                <TextBlock Text="{loc:Loc O365Edit_Field_UsernameDesc}" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,16"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="{loc:Loc Common_Save}" Style="{StaticResource ModernButton}" Click="BtnSave_Click" Width="120" Margin="0,0,12,0"/>
                <Button Content="{loc:Loc Common_Cancel}" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click" Width="120"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>